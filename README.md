# Simple RAG Chatbot with UI

## This is a simple Python + JS RAG Chatbot with UI created with Pinecone and Google Gemini

It is designed to be used on the web (JS React).

Can be used as a template to add more features